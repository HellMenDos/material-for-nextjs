/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  appDir: true,
  images:{
    domains: ['source.unsplash.com','nextjs.org']
  }
}

module.exports = nextConfig
